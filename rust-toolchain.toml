# Rust toolchain configuration
#
# Pin the default toolchain for development.
# CI uses its own matrix for MSRV testing.

[toolchain]
channel = "stable"
components = ["rustfmt", "clippy"]
targets = []
profile = "default"
